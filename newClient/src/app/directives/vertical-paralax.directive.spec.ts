import { VerticalParalaxDirective } from './vertical-paralax.directive';

describe('VerticalParalaxDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalParalaxDirective();
    expect(directive).toBeTruthy();
  });
});
